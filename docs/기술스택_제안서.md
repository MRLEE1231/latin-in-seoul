# Latin in Seoul — 기술 스택 제안서

## 프로젝트 특성 분석

- **복잡도**: 중하위 (게시판 CRUD + 이미지 업로드)
- **트래픽 예상**: 초기 소규모 → 중장기 확장 가능
- **개발 속도**: 빠른 프로토타입 → 점진적 개선
- **유지보수**: 장기 운영 고려

---

## 제안 옵션 비교

### 옵션 1: 기존 스킬 활용 (빠른 개발) ⚡

| 계층 | 기술 스택 | 비고 |
|------|-----------|------|
| **프론트엔드** | HTML + CSS + JavaScript + jQuery | 서버사이드 렌더링 |
| **백엔드** | Spring Boot + Spring MVC | REST API 또는 전통적 MVC |
| **템플릿 엔진** | Thymeleaf | Spring과 통합 용이 |
| **데이터베이스** | Oracle (기존) 또는 MySQL/PostgreSQL | Oracle 라이선스 고려 |
| **이미지 저장** | 로컬 파일시스템 또는 Oracle BLOB | 초기엔 로컬, 확장 시 S3/클라우드 |

#### 장점 ✅
- **즉시 개발 가능** (학습 곡선 최소)
- **검증된 스킬셋** 활용
- **빠른 프로토타입** 완성
- **Spring Boot 생태계** 풍부 (이미지 처리, 파일 업로드 등)

#### 단점 ⚠️
- **프론트엔드 한계**: jQuery 기반 → 현대적 UX 구현 어려움
- **확장성**: SPA 구조로 전환 시 리팩토링 필요
- **Oracle 비용**: 상용 라이선스 (개인/소규모 프로젝트 부담)

#### 추천 시나리오
- **1-2주 내 MVP** 필요
- **개인 프로젝트** 또는 **소규모 팀**
- **프론트엔드 학습 시간 없음**

---

### 옵션 2: 현대적 스택 (확장성/학습) 🚀

| 계층 | 기술 스택 | 비고 |
|------|-----------|------|
| **프론트엔드** | React + TypeScript | 또는 Vue 3 |
| **스타일링** | Tailwind CSS 또는 CSS Modules | 빠른 UI 개발 |
| **상태관리** | React Query (서버 상태) + Zustand (클라이언트) | 또는 Context API |
| **백엔드** | Spring Boot + Spring Data JPA | REST API |
| **데이터베이스** | PostgreSQL 또는 MySQL | 오픈소스, 무료 |
| **이미지 저장** | AWS S3 또는 Cloudflare R2 | 또는 로컬 → 클라우드 전환 |
| **빌드 도구** | Vite (프론트) + Maven/Gradle (백엔드) | 빠른 개발 서버 |

#### 장점 ✅
- **확장성**: SPA 구조 → 모바일 앱 전환 용이
- **현대적 UX**: 컴포넌트 기반, 반응형 디자인
- **학습 가치**: 시장 수요 높은 스킬
- **오픈소스 DB**: 비용 절감
- **API 중심**: 프론트/백 분리 → 독립 배포 가능

#### 단점 ⚠️
- **학습 시간**: React/TypeScript 학습 필요 (1-2주)
- **초기 설정**: 프론트/백 분리 → 개발 환경 복잡도 증가
- **배포 복잡도**: 프론트 빌드 + 백엔드 배포 분리

#### 추천 시나리오
- **장기 운영** 계획
- **스킬 확장** 목적
- **현대적 UX** 중요
- **2-3주 개발 시간** 확보 가능

---

### 옵션 3: Next.js 풀스택 (서버사이드 렌더링) ⚡🚀

| 계층 | 기술 스택 | 비고 |
|------|-----------|------|
| **프론트엔드** | Next.js 14+ (App Router) + TypeScript | React 기반 풀스택 |
| **백엔드** | Next.js API Routes 또는 Spring Boot API | 선택 가능 |
| **스타일링** | Tailwind CSS | 빠른 UI 개발 |
| **데이터베이스** | PostgreSQL/MySQL | Prisma 또는 직접 쿼리 |
| **ORM** | Prisma 또는 Drizzle | 또는 Spring Data JPA (백엔드 분리 시) |
| **이미지 저장** | Next.js Image Optimization + S3/로컬 | 자동 최적화 |

#### Next.js 사용 방식 (2가지 선택)

**방식 A: Next.js 풀스택** (추천)
- Next.js API Routes로 백엔드까지 처리
- 단일 프로젝트로 관리
- 배포 단순화 (Vercel 등)

**방식 B: Next.js 프론트 + Spring Boot API**
- Next.js는 프론트엔드만 (SSR/SSG)
- Spring Boot는 REST API 제공
- 백엔드 로직은 Java로 유지

#### 장점 ✅
- **SEO 최적화**: 서버사이드 렌더링 (SSR) → 검색 엔진 친화적
- **빠른 초기 로딩**: SSG/ISR로 정적 페이지 생성 가능
- **백엔드 개발자 친화**: API Routes가 서버 로직과 유사
- **자동 최적화**: 이미지, 폰트, 번들 최적화 자동
- **배포 간편**: Vercel 배포 매우 쉬움 (무료 티어 제공)
- **프로젝트 단순화**: 프론트/백 통합 관리 가능

#### 단점 ⚠️
- **React 학습 필요**: Next.js는 React 기반 (1-2주 학습)
- **Node.js 생태계**: Java 개발자에게 낯선 환경
- **Spring Boot 미사용**: 백엔드를 Next.js로 하면 Java 스킬 활용 못함
- **복잡도**: SSR/SSG/CSR 개념 이해 필요

#### 학습 난이도 평가 📊

| 항목 | 난이도 | 비고 |
|------|--------|------|
| **React 기초** | ⭐⭐⭐ (중) | 컴포넌트, 훅 개념 이해 필요 |
| **Next.js 기본** | ⭐⭐ (하-중) | 파일 기반 라우팅 직관적 |
| **API Routes** | ⭐⭐ (하) | Express.js와 유사, 백엔드 개발자 친화 |
| **SSR/SSG 개념** | ⭐⭐⭐ (중) | 초기 이해 필요하지만 문서 풍부 |
| **전체 학습 시간** | **2-3주** | 기본 기능 구현 가능 수준 |

#### Java 개발자 관점에서의 Next.js

**친숙한 부분** ✅
- API Routes: 서버 로직 작성 (Express.js 스타일)
- 파일 기반 구조: 컨트롤러/서비스 패턴과 유사
- 타입 안정성: TypeScript 사용 시 Java와 유사

**낯선 부분** ⚠️
- JavaScript/TypeScript 문법
- React 컴포넌트 개념 (JSX)
- 비동기 처리 (Promise, async/await)

#### 추천 시나리오
- **SEO 중요** (게시판 → 검색 노출)
- **빠른 배포** 원함 (Vercel 무료)
- **단일 프로젝트** 선호
- **React 학습 의지** 있음 (2-3주)
- **Java 백엔드 유지** 원하면 → 방식 B 선택

---

### 옵션 4: 하이브리드 (점진적 전환) 🔄

| 계층 | 기술 스택 | 비고 |
|------|-----------|------|
| **프론트엔드** | Thymeleaf (초기) → React (점진적) | 또는 Vue |
| **백엔드** | Spring Boot + REST API | 프론트와 독립 |
| **데이터베이스** | MySQL/PostgreSQL | Oracle 대체 |
| **이미지 저장** | 로컬 → 클라우드 전환 | 초기엔 로컬 |

#### 전략
1. **Phase 1**: Thymeleaf로 빠르게 MVP 완성
2. **Phase 2**: REST API 구축 (기존 백엔드 활용)
3. **Phase 3**: React 프론트엔드로 점진적 전환

#### 장점 ✅
- **빠른 시작** + **점진적 개선**
- **리스크 최소화**: 단계별 전환
- **기존 스킬 활용** + **새 기술 학습**

#### 단점 ⚠️
- **중간 단계** 유지보수 부담
- **전환 비용** 발생

---

## 최종 추천안 🎯

### 추천 순위

1. **옵션 3 (Next.js)** - SEO + 빠른 배포 + 현대적 스택
2. **옵션 2 (React + Spring Boot)** - 확장성 + Java 스킬 활용
3. **옵션 1 (Thymeleaf + jQuery)** - 즉시 개발 가능

#### 상황별 선택 가이드

| 상황 | 추천 옵션 | 이유 |
|------|-----------|------|
| **빠른 MVP 필요** (1-2주) | 옵션 1 | 즉시 개발 가능 |
| **SEO 중요 + 빠른 배포** | 옵션 3 (Next.js) | SSR/SSG + Vercel 배포 |
| **Java 백엔드 유지 + 학습** | 옵션 2 또는 옵션 3-B | Spring Boot 활용 |
| **단일 프로젝트 선호** | 옵션 3 (Next.js 풀스택) | 프론트/백 통합 |
| **점진적 접근** | 옵션 4 | 리스크 분산 |

---

## 상세 기술 스택

### 옵션 3: Next.js 풀스택 상세

#### 프론트엔드 + 백엔드

```json
{
  "framework": "Next.js 14+ (App Router)",
  "language": "TypeScript",
  "styling": "Tailwind CSS",
  "state": "React Server Components + React Query (클라이언트)",
  "routing": "파일 기반 라우팅 (자동)",
  "database": "Prisma ORM + PostgreSQL",
  "image": "next/image (자동 최적화)"
}
```

**주요 라이브러리**
- `next`: 프레임워크 코어
- `prisma`: ORM (데이터베이스 접근)
- `react-query`: 클라이언트 상태 관리 (필터, UI)
- `react-hook-form`: 폼 관리
- `zod`: 타입 안전한 스키마 검증

**프로젝트 구조 (Next.js App Router)**
```
app/
├── page.tsx              # 홈 (게시판 목록)
├── posts/
│   ├── [id]/
│   │   └── page.tsx      # 상세 페이지
│   └── new/
│       └── page.tsx      # 작성 페이지 (관리자)
├── api/
│   └── posts/
│       ├── route.ts      # GET/POST /api/posts
│       └── [id]/
│           └── route.ts  # GET/PUT/DELETE /api/posts/:id
└── components/           # 재사용 컴포넌트
```

**Next.js 방식 A: 풀스택 (API Routes 사용)**
```typescript
// app/api/posts/route.ts
export async function GET(request: Request) {
  const posts = await prisma.post.findMany();
  return Response.json(posts);
}

export async function POST(request: Request) {
  const body = await request.json();
  const post = await prisma.post.create({ data: body });
  return Response.json(post);
}
```

**Next.js 방식 B: Spring Boot API 연동**
```typescript
// app/api/posts/route.ts (또는 클라이언트 컴포넌트)
const response = await fetch('http://localhost:8080/api/posts');
const posts = await response.json();
```

---

### 옵션 2: React + Spring Boot 상세

#### 프론트엔드

```json
{
  "framework": "React 18+",
  "language": "TypeScript",
  "styling": "Tailwind CSS",
  "state": "React Query + Zustand",
  "routing": "React Router",
  "build": "Vite",
  "image": "react-image-gallery 또는 swiper"
}
```

**주요 라이브러리**
- `react-query`: 서버 상태 관리 (게시글 목록/상세)
- `zustand`: 클라이언트 상태 (필터, UI 상태)
- `react-hook-form`: 폼 관리 (관리자 작성/수정)
- `axios`: HTTP 클라이언트

### 백엔드

```yaml
Framework: Spring Boot 3.x
Language: Java 17+
ORM: Spring Data JPA
API: REST API (JSON)
File Upload: MultipartFile
Image Processing: Thumbnailator (썸네일 생성)
```

**주요 의존성**
- `spring-boot-starter-web`
- `spring-boot-starter-data-jpa`
- `spring-boot-starter-validation`
- `mysql-connector-java` 또는 `postgresql`

### 데이터베이스

**추천: PostgreSQL** (또는 MySQL)

```sql
-- 게시글 테이블
CREATE TABLE posts (
    id BIGSERIAL PRIMARY KEY,
    title VARCHAR(255),
    content TEXT,
    region VARCHAR(20), -- 'GANGNAM', 'HONGDAE', 'ETC'
    dance_type VARCHAR(20), -- 'SALSA', 'BACHATA', 'KIZOMBA'
    instructor_name VARCHAR(100),
    class_description TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP
);

-- 이미지 테이블
CREATE TABLE post_images (
    id BIGSERIAL PRIMARY KEY,
    post_id BIGINT REFERENCES posts(id),
    image_url VARCHAR(500),
    image_order INT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 이미지 저장 전략

**초기 (로컬)**
```
/upload/images/
  ├── {postId}/
  │   ├── original/
  │   └── thumbnail/
```

**확장 (클라우드)**
- AWS S3
- Cloudflare R2 (S3 호환, 저렴)
- Naver Cloud Platform Object Storage

---

## 개발 환경 설정

### 옵션 3: Next.js 풀스택

```bash
# Next.js 프로젝트 생성
npx create-next-app@latest latin-in-seoul --typescript --tailwind --app

cd latin-in-seoul

# 추가 라이브러리 설치
npm install prisma @prisma/client
npm install @tanstack/react-query zustand react-hook-form zod
npm install @hookform/resolvers

# Prisma 초기화
npx prisma init

# 개발 서버 실행
npm run dev
# → http://localhost:3000
```

**Prisma 설정 (prisma/schema.prisma)**
```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Post {
  id               BigInt      @id @default(autoincrement())
  title            String?
  content          String
  region           String?     // 'GANGNAM', 'HONGDAE', 'ETC'
  danceType        String?     // 'SALSA', 'BACHATA', 'KIZOMBA'
  instructorName   String?
  classDescription String?
  createdAt        DateTime    @default(now())
  updatedAt        DateTime    @updatedAt
  images           PostImage[]
}

model PostImage {
  id        BigInt   @id @default(autoincrement())
  postId    BigInt
  post      Post     @relation(fields: [postId], references: [id])
  imageUrl  String
  imageOrder Int     @default(0)
  createdAt DateTime @default(now())
}
```

---

### 옵션 2: React + Spring Boot

#### 프론트엔드
```bash
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install
npm install react-query zustand react-router-dom axios
npm install -D tailwindcss
```

#### 백엔드
```bash
# Spring Initializr 사용
- Dependencies: Web, JPA, Validation, MySQL/PostgreSQL
```

---

## API 설계 (REST)

```
GET    /api/posts              # 목록 조회 (필터링 지원)
GET    /api/posts/{id}         # 상세 조회
POST   /api/posts              # 작성 (관리자)
PUT    /api/posts/{id}         # 수정 (관리자)
DELETE /api/posts/{id}         # 삭제 (관리자)
POST   /api/posts/{id}/images  # 이미지 업로드
```

---

## Next.js 학습 가이드 (Java 개발자용)

### 학습 순서 추천

1. **JavaScript/TypeScript 기초** (3-5일)
   - 변수, 함수, 객체, 배열
   - async/await (비동기 처리)
   - 타입스크립트 기본 타입

2. **React 기초** (5-7일)
   - 컴포넌트, JSX
   - useState, useEffect 훅
   - Props 전달

3. **Next.js 기본** (3-5일)
   - 파일 기반 라우팅
   - Server Components vs Client Components
   - API Routes

4. **실전 프로젝트** (나머지 시간)
   - 게시판 구현하며 학습

### Java 개발자 관점에서의 Next.js

**비유로 이해하기**
```
Java Spring Boot          →  Next.js
─────────────────────────────────────
@Controller              →  app/api/posts/route.ts
@Service                 →  API Route 내부 로직
@Repository              →  Prisma Client
application.properties   →  .env
Maven/Gradle            →  npm/pnpm
```

**API Routes 예시 (Spring Controller와 유사)**
```typescript
// Spring: @RestController
@GetMapping("/api/posts")
public List<Post> getPosts() { ... }

// Next.js: app/api/posts/route.ts
export async function GET() {
  const posts = await prisma.post.findMany();
  return Response.json(posts);
}
```

### Next.js가 어려운 이유 vs 쉬운 이유

**어려운 부분** ⚠️
- React 개념 (컴포넌트, 훅) 처음 접하면 생소
- JavaScript 비동기 처리 (Promise, async/await)
- 클라이언트/서버 컴포넌트 구분

**쉬운 부분** ✅
- 파일 기반 라우팅 (직관적)
- API Routes (백엔드 개발자에게 친숙)
- TypeScript 타입 안정성 (Java와 유사)
- 공식 문서가 매우 잘 되어 있음

### 결론: Next.js 난이도

**전체 평가: ⭐⭐⭐ (중간)**

- **React 기초만 알면**: ⭐⭐ (쉬움)
- **React 모르면**: ⭐⭐⭐⭐ (보통-어려움)

**추천**: 
- React 튜토리얼 1주일 + Next.js 튜토리얼 3일 → **총 2주면 기본 구현 가능**
- 또는 Next.js 공식 튜토리얼 따라하기 (React 기초 포함)

---

## 다음 단계

1. **스택 확정**: 옵션 선택 (1/2/3/4)
2. **프로젝트 초기화**: 프론트/백엔드 프로젝트 생성
3. **DB 스키마 설계**: 상세 테이블 구조 정의
4. **API 명세 작성**: OpenAPI/Swagger 문서화
5. **개발 시작**: 백엔드 API → 프론트엔드 연동

---

*작성일: 2025-02-12 | 기술 스택 제안 v2 (Next.js 옵션 추가)*
